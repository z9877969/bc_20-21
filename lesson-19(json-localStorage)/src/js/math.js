'use strict';

export const add = (a, b) => {
  return a + b;
};

export const sub = (a, b) => {
  return a - b;
};

export const division = (a, b) => {
  return a / b;
};

export const multi = (a, b) => {
  return a * b;
};

// const mathFns = {
//   add,
//   sub,
//   division,
// };

// console.log('object');

// export default mathFns;
